# VBF Premium – frontend

React (Vite) SPA. Üzemeltetés és teljes dokumentáció: a repó gyökerében lévő `README.md`.

```bash
npm install && npm run dev   # fejlesztés
npm run build                # éles bundle
```
