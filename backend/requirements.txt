# Core framework
fastapi
uvicorn[standard]
sqlalchemy
pydantic
# PostgreSQL (ha DATABASE_URL postgresql://...)
psycopg2-binary
# Redis (rate limit, lock)
redis
pydantic-settings
python-multipart

# Auth & Security
passlib[bcrypt]
bcrypt==4.0.1
python-jose[cryptography]
slowapi

# Document generation
python-docx
docx2pdf
pyhanko
Pillow
openpyxl
qrcode[pil]

# Testing (dev only)
pytest
httpx

# Payments
stripe

# Optional: error tracking (pip install sentry-sdk, then set SENTRY_DSN)
# sentry-sdk
