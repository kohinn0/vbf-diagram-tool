# Elavult leírás — ne használd elsődleges forrásként

Ez a fájl korábban a **három külön HTML entrypoint** (`index.html` / `app.html` / `shop.html`) felépítését dokumentálta. A jelenlegi alkalmazás **egyetlen React + Vite SPA** (`frontend/`), egyetlen builddel.

**Aktuális áttekintés:** `PROGRAM_LEIRAS.md`, onboarding és szabályok: `AI_CONTEXT.md`, áttekintő: `README.md`.
